# TriVision — Runtime Requirements
# Core (required)
PyQt6>=6.5.0
opencv-python>=4.9.0
numpy>=1.24.0

# scikit-image (strongly recommended — unlocks ~40 additional algorithms)
scikit-image>=0.21.0
scipy>=1.11.0

# Optional extras
tqdm>=4.66.0       # progress bars in batch processing
requests>=2.31.0   # future: model downloading
