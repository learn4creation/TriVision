# TriVision Web UI Dependencies
# These are needed in addition to (or instead of) the desktop requirements

# Web framework
flask>=3.0.0
flask-socketio>=5.3.0      # real-time webcam streaming to browser

# Image encoding for browser transfer
Pillow>=10.0.0

# Core CV (headless — no Qt needed)
opencv-python-headless>=4.9.0
numpy>=1.24.0
scikit-image>=0.21.0
scipy>=1.11.0

# Async server for SocketIO
eventlet>=0.35.0

# Utilities
tqdm>=4.66.0
